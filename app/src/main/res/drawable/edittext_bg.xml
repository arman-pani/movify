<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <padding android:right="16dp"  android:bottom="12dp" android:left="16dp" android:top="12dp" />
    <solid android:color="#000000" /> <!-- Background color -->
    <corners android:radius="0dp" />
    <stroke android:width="0dp" android:color="@android:color/transparent" /> <!-- No underline -->
</shape>
